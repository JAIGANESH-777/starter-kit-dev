# Project Spec: my-app-2

> **Generated by Accelerator Enterprise Architecture Compiler** on 15 Jun 2026, 17:45
> Paste this file into Claude Code, Codex, or Cursor and let it scaffold the full repository.

---

## 1. Overview

| Field | Value |
|:---|:---|
| **Project Name** | `my-app-2` |
| **Type** | `fullstack` |
| **Primary Language** | `TypeScript` |
| **Description** | *demo* |

---

## 2. Frontend

| Field | Value |
|:---|:---|
| **Framework** | `Next.js` |
| **Styling** | `Tailwind CSS + shadcn/ui` |
| **State Management** | `Zustand` |
| **Data Fetching** | `TanStack Query + Axios` |
| **Form Handling** | `React Hook Form + Zod` |

### Required Environment Variables

```env
# frontend/.env.local
NEXT_PUBLIC_API_URL=http://localhost:8000/api
```

> **SSR Note** (Next.js / Nuxt / SvelteKit): server-side fetches inside Docker must use the backend service name,
> e.g. `http://backend:8000/api`. Client-side fetches always use `localhost`.

### Cookie Credentials Transmission (mandatory for auth)
- Set client-side fetching clients to send cookies automatically with requests:
  - **Axios**: set `axios.defaults.withCredentials = true` globally or pass `{ withCredentials: true }` in Axios instance options.
  - **Fetch API**: pass `{ credentials: 'include' }` in options block.
  - **SWR**: configure the fetcher parameter to pass `{ credentials: 'include' }`.
  - **TanStack Query**: configure query/mutation fetch functions to use credentials options.

### Framework Bootstrap Command
Execute this command to scaffold the frontend project framework:
```bash
npx create-next-app@latest frontend --ts --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm
```

### Design System Adapter Bindings
- Read colors, spacing, and typography from `shared_tokens.md` before writing any CSS.
- **W-4 FIX**: Convert HEX values in `shared_tokens.md` to CSS HSL fragments so Tailwind maps correctly.
- Implement a `data-theme` dark/light toggle wired to the token system.

---

## 3. Backend

| Field | Value |
|:---|:---|
| **Framework** | `NestJS` |
| **API Style** | `REST + GraphQL` |
| **Validation** | `class-validator` |

### Features Enabled
- Health Checks
- Rate Limiting
- Redis Caching

### Required Environment Variables

```env
# backend/.env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/appdb
JWT_SECRET=change-me-in-production
PORT=8000
REDIS_URL=redis://localhost:6379/0
```

### Framework Bootstrap Command
Execute this command to scaffold the backend project framework:
```bash
npx @nestjs/cli new backend --package-manager npm
```

### Environment Variables Validation
```typescript
// Validate environment variables at startup using Zod or Envalid
import { cleanEnv, str, port } from "envalid";

export const env = cleanEnv(process.env, {
  DATABASE_URL: str(),
  JWT_SECRET: str(),
  PORT: port({ default: 8000 }),
});
```

### Module Pattern
```
backend/src/{module}/{module}.controller.ts
backend/src/{module}/{module}.service.ts
backend/src/{module}/{module}.module.ts
backend/src/common/          # shared guards, filters, interceptors, pipes
backend/src/config/          # environment config module
```

### Security & Transport Baselines
- **Security Headers (Helmet)**: Install `helmet` and register it globally in `main.ts` via `app.use(helmet())`.
- **Cookie Parser**: Install `@fastify/cookie` (if using Fastify) or `cookie-parser` (if using Express), register it globally, and configure `JwtStrategy` to extract tokens from cookies (`request.cookies.token`).


### Feature Integration Instructions
> These features must be **wired up with actual code**, not just installed as packages.
#### Redis Caching
- Install: `@nestjs/cache-manager cache-manager ioredis`
- Create `src/cache/cache.module.ts` — register `CacheModule.registerAsync()` configured from `REDIS_URL`.
- Import `CacheModule` (global: true) in `app.module.ts`.
#### Redis Connection Resilience
- **Auto-Reconnect**: Configure client options to automatically reconnect when connection is dropped (e.g. in `ioredis` set `retryStrategy` option).
#### Rate Limiting
- Install: `@nestjs/throttler`
- Register `ThrottlerModule.forRoot([{ ttl: 60000, limit: 100 }])` globally in `app.module.ts` and bind the `ThrottlerGuard` globally (provider: `APP_GUARD`, useClass: `ThrottlerGuard`).
- Apply `@Throttle({ default: { limit: 5, ttl: 900000 } })` decorators on authentication endpoints (e.g. login, verify-otp) to enforce a strict limit of 5 requests per 15 minutes.

### Health Endpoint (L-2 Rule)
- Route: `GET /api/health` → `{ status: "ok" }` with HTTP 200
- **Verify this returns 200 before writing a single frontend component.**

---

## 4. Database

| Field | Value |
|:---|:---|
| **Databases** | `MongoDB` |
| **ORM / Query Layer** | `Mongoose` |
| **Migrations** | `Yes` |
| **Seed Scripts** | `Yes` |

### Connection Rules
- Use the standard connection URL format for your database driver.


- MongoDB URI: `mongodb://user:pass@mongo:27017/dbname` inside Docker.


### Connection Pooling Configurations
To comply with production performance standards, configure connection pooling:
- Limit connections using driver configuration parameters (e.g. `max: 10` for pg, `poolSize: 10` for mongodb).

### Migration Commands
- Run: _No ORM migration tool — manage manually_
- Run seed scripts immediately after migrations.

### Docker Compose DB Reference
```yaml
services:
  db:
    image: mongo:7
    environment:
      MONGO_INITDB_DATABASE: appdb
    ports:
      - "27017:27017"
```

---

## 5. Authentication & Authorization

| Field | Value |
|:---|:---|
| **Strategy** | `Hybrid Multi-Auth` |
| **Implementation** | `Auth.js` |
| **RBAC** | `Yes` |
| **Multi-Tenant** | `No` |

### Auth Methods
- Google OAuth
- Azure AD OAuth

### Required Environment Variables

```env
# backend/.env
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
AZURE_AD_TENANT_ID=
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
# frontend/.env.local
NEXTAUTH_SECRET=change-me-in-production
NEXTAUTH_URL=http://localhost:3000
```

### Implementation Rules
- Protect all non-public routes with auth guards/middleware.
- Store tokens in HTTP-only cookies — never in localStorage. Set the `HttpOnly`, `Secure` (production only), and `SameSite=Strict` flags on the auth cookie to prevent session leakage and hijacking.
- Refresh tokens must be rotated on every use.


### Frontend Auth UI Requirements
> Implement **every** selected auth method with its own distinct UI. Do not default to email+password for all methods.
- **Google OAuth**: "Continue with Google" branded button with Google logo. Redirects to `/api/auth/google`; handles callback at `/api/auth/google/callback`.
- **Azure AD OAuth**: "Sign in with Microsoft" branded button. Use MSAL or provider redirect flow.



### Roles (RBAC)
Define and enforce these roles via guards/middleware:
- admin
- manager
- editor
- viewer

---

## 6. Infrastructure & DevOps

| Field | Value |
|:---|:---|
| **Cloud / Platform** | `Azure` |
| **Terraform (IaC)** | `Yes` |
| **Docker** | `Yes` |
| **CI/CD** | `Yes — GitHub Actions` |

### Docker Setup
- Multi-stage Dockerfile
- docker-compose
- .dockerignore
- Docker Healthchecks

### Docker Port Rules (C-1 — mandatory)
- Frontend: `EXPOSE 3000` — process must listen on port 3000
- Backend: `EXPOSE 8000` — process must listen on port 8000
- Use service names (`backend`, `db`, `redis`) for inter-container communication, never `localhost`
- Add `depends_on: { <service>: { condition: service_healthy } }` to avoid race conditions

### Docker Compose Template

Place this file at the **project root** as `docker-compose.yml`.
The agent **must not** modify service names, internal hostnames, or port mappings.

```yaml
version: '3.9'

services:
  mongo:
    image: mongo:7
    container_name: app_mongo
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_DATABASE: appdb
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7-alpine
    container_name: app_redis
    ports:
      - '6379:6379'
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
    container_name: app_backend
    command: npm run start:prod
    ports:
      - '8000:8000'
    environment:
      - PORT=8000
      - JWT_SECRET=change-me-in-production
      - ALLOWED_ORIGINS=http://localhost:3000
      - DATABASE_URL=mongodb://mongo:27017/appdb
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      mongo:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
    container_name: app_frontend
    ports:
      - '3000:3000'
    environment:
      - NEXT_PUBLIC_API_URL=http://backend:8000/api
    depends_on:
      - backend

volumes:
  mongo_data:

```

> **C-1 Rule**: All inter-container hostnames must use Docker service names (`backend`, `db`, `redis`, `mongo`, `keycloak`),
> never `localhost`. Frontend SSR fetches inside Docker also use service names, not `localhost`.

### .dockerignore Templates
Configure `.dockerignore` files to prevent transferring large directories like `node_modules` and local environment files during Docker builds.

#### backend/.dockerignore
```text
node_modules
npm-debug.log
dist
.env
.env.development
.env.production
```

#### frontend/.dockerignore
```text
node_modules
.next
out
npm-debug.log
.env.local
.env.development.local
.env.production.local
```

### CI/CD Pipeline (GitHub Actions)
Create this configuration at the root of your project.

```yaml
# .github/workflows/ci.yml
name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"
          cache: "npm"
      - name: Install Dependencies
        run: npm ci
      - name: Run Linters
        run: npm run lint
      - name: Type Check
        run: npm run typecheck
```

### Code Quality Tools
- ESLint
- Prettier
- Husky
- lint-staged
- Commitlint

- **ESLint**: `eslint.config.mjs` (flat config). Use `@typescript-eslint/recommended` + framework plugin.
- **Prettier**: `.prettierrc` → `{ "singleQuote": true, "semi": true, "printWidth": 100 }`.
- **Husky**: `npx husky init` → add `pre-commit` (lint-staged) and `commit-msg` (commitlint) hooks.
- **lint-staged**: in `package.json` → run ESLint + Prettier on staged `*.ts` / `*.tsx` files.
- **Commitlint**: `@commitlint/config-conventional` + commit-msg Husky hook.

#### eslint.config.mjs (ESLint Flat Config)
```javascript
import js from "@eslint/js";
import ts from "@typescript-eslint/eslint-plugin";
import tsParser from "@typescript-eslint/parser";

export default [
  js.configs.recommended,
  {
    files: ["**/*.ts", "**/*.tsx"],
    languageOptions: {
      parser: tsParser,
      parserOptions: {
        ecmaVersion: "latest",
        sourceType: "module",
      },
    },
    plugins: {
      "@typescript-eslint": ts,
    },
    rules: {
      "@typescript-eslint/no-unused-vars": "warn",
      "no-console": ["warn", { allow: ["warn", "error", "info"] }],
    },
  },
];
```

#### .prettierrc (Prettier Formatting Configuration)
```json
{
  "singleQuote": true,
  "semi": true,
  "printWidth": 100,
  "tabWidth": 2,
  "trailingComma": "all"
}
```

#### lint-staged (Add to package.json)
```json
{
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
```

### Terraform Infrastructure Configuration

Place the IaC manifest in the `terraform/` directory.

```hcl
# terraform/main.tf
provider "azurerm" {
  features {}
}

terraform {
  required_version = ">= 1.5.0"
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 3.0"
    }
  }
}

resource "azurerm_resource_group" "main" {
  name     = "app-resources"
  location = "East US"
}
```

---

### Docker Compose Template

Place this file at the **project root** as `docker-compose.yml`.
The agent **must not** modify service names, internal hostnames, or port mappings.

```yaml
version: '3.9'

services:
  mongo:
    image: mongo:7
    container_name: app_mongo
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_DATABASE: appdb
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7-alpine
    container_name: app_redis
    ports:
      - '6379:6379'
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
    container_name: app_backend
    command: npm run start:prod
    ports:
      - '8000:8000'
    environment:
      - PORT=8000
      - JWT_SECRET=change-me-in-production
      - ALLOWED_ORIGINS=http://localhost:3000
      - DATABASE_URL=mongodb://mongo:27017/appdb
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      mongo:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
    container_name: app_frontend
    ports:
      - '3000:3000'
    environment:
      - NEXT_PUBLIC_API_URL=http://backend:8000/api
    depends_on:
      - backend

volumes:
  mongo_data:

```

> **C-1 Rule**: All inter-container hostnames must use Docker service names (`backend`, `db`, `redis`, `mongo`, `keycloak`),
> never `localhost`. Frontend SSR fetches inside Docker also use service names, not `localhost`.

## 7. Testing & Quality Assurance

| Field | Value |
|:---|:---|
| **Testing Framework** | `Vitest` |
| **Coverage Enforced** | `No` |

### Test Types
- Unit tests
- Integration tests
- API tests




---

## 9. Project Directory Structure

The agent **must** scaffold the repository using exactly this folder layout.
Do not invent directories or deviate from this structure without explicit instruction.

```text
my-app-2/
├── CLAUDE.md
├── SPEC.md
├── shared_tokens.md
├── .env.example
├── .gitignore
├── README.md
├── eslint.config.mjs
├── .prettierrc
├── commitlint.config.js
├── .husky/
│   ├── pre-commit
│   └── commit-msg
├── assets/
│   ├── logo.svg
│   ├── logo-mark.svg
│   ├── favicon.ico
│   └── fonts/
├── docker-compose.yml
├── .github/
│   └── workflows/
│       └── ci.yml
├── terraform/
│   ├── main.tf
│   ├── variables.tf
│   ├── outputs.tf
│   └── modules/
├── backend/
│   ├── src/
│   │   ├── main.ts
│   │   ├── app.module.ts
│   │   ├── common/
│   │   │   ├── guards/
│   │   │   ├── filters/
│   │   │   ├── interceptors/
│   │   │   └── pipes/
│   │   ├── config/
│   │   │   └── configuration.ts
│   │   ├── health/
│   │   │   ├── health.controller.ts
│   │   │   └── health.module.ts
│   │   ├── auth/
│   │   │   ├── auth.controller.ts
│   │   │   ├── auth.service.ts
│   │   │   ├── auth.module.ts
│   │   │   ├── dto/
│   │   │   └── strategies/
│   │   │       └── jwt.strategy.ts
│   │   └── users/
│   │       ├── users.controller.ts
│   │       ├── users.service.ts
│   │       ├── users.module.ts
│   │       └── dto/
│   ├── test/
│   ├── .env.example
│   ├── package.json
│   ├── tsconfig.json
│   ├── Dockerfile
│   └── .dockerignore
└── frontend/
    ├── app/
    │   ├── layout.tsx
    │   ├── page.tsx
    │   ├── globals.css
    │   ├── (auth)/
    │   │   ├── login/
    │   │   │   └── page.tsx
    │   │   └── register/
    │   │       └── page.tsx
    │   └── (dashboard)/
    │       ├── layout.tsx
    │       └── page.tsx
    ├── components/
    │   ├── ui/
    │   ├── layouts/
    │   └── forms/
    ├── lib/
    │   ├── api.ts
    │   └── utils.ts
    ├── hooks/
    ├── store/
    ├── types/
    ├── public/
    ├── .env.local.example
    ├── next.config.ts
    ├── tsconfig.json
    ├── tailwind.config.ts
    ├── Dockerfile
    └── .dockerignore
```

> Directory and file names with `{resource}` or `{module}` are placeholders.
> Replace them with actual domain entity names (e.g. `products/`, `orders/`).

---

## 10. AI Scaffold Instructions

You are an expert full-stack systems engineering agent.
Read `CLAUDE.md`, `SPEC.md`, and `shared_tokens.md` **completely** before writing a single line of code.
Scaffold the repository following this pipeline exactly — do not skip steps or reorder them.

### Pipeline

1. **Root Foundations**
   - Create `.gitignore`, `README.md`, root `package.json` / `pyproject.toml`.
   - Generate `backend/.env.example` and `frontend/.env.local.example` with all variables from Sections 3 and 5.
   - Configure code quality tools: ESLint, Prettier, Husky, lint-staged, Commitlint.

2. **Database Schema** *(MongoDB via Mongoose)*
   - Write schema / model files.
   - Generate initial migration.
   - Write deterministic seed scripts.

3. **Core Backend — L-2 Rule** *(NestJS)*
   - Scaffold app bootstrap, module structure, shared middleware.
   - Implement `GET /api/health` → `{ status: "ok" }`.
   - **⛔ Do not write frontend code until `curl http://localhost:8000/api/health` returns HTTP 200.**

4. **Auth Layer** *(Auth.js — Google OAuth, Azure AD OAuth)*
   - Wire up Auth.js with: Google OAuth, Azure AD OAuth.
   - Apply auth guards to all non-public routes.
   - Implement RBAC guards for roles: admin, manager, editor, viewer.

5. **Frontend Scaffold** *(Next.js)*
   - Bootstrap with non-interactive CLI flags.
   - Apply design tokens from `shared_tokens.md` — convert HEX → HSL.
   - Implement dark/light theme toggle via `data-theme`.
   - Dashboard shell with live connection indicator calling `/api/health`.

6. **Docker Orchestration** *(enabled)*
   - Copy the `docker-compose.yml` template from Section 6 **verbatim** to the project root.
   - Do not rename services, change internal hostnames, or alter port mappings.
   - Write multi-stage Dockerfiles: frontend `EXPOSE 3000`, backend `EXPOSE 8000`.
   - Write `.dockerignore` files for both backend and frontend as detailed in Section 6 to ignore `node_modules`, environment files, and build outputs.

7. **CI/CD** *(GitHub Actions — Lint, Type check)*
   - Generate pipeline config running: Lint, Type check.

8. **Tests** *(Vitest — Unit tests, Integration tests, API tests)*
   - Scaffold test files alongside source files.
   

9. **README.md**
   - Local setup: clone → install → env setup → migrate → run
   - All environment variables documented
   - How to run tests and Docker

### Critical Rules
- Never hardcode secrets — use environment variables exclusively.
- Never use `localhost` as a hostname inside Docker containers — use service names.
- Follow the L-2 rule: backend `/health` endpoint verified before any frontend code is written.
- Follow Section 9 directory structure exactly.
- For Docker: copy the compose template from Section 6 verbatim — do not invent or remove services.