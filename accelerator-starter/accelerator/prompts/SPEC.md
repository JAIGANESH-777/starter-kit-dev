# Project Spec: Python-app

> **Generated by Accelerator Enterprise Architecture Compiler** on 17 Jun 2026, 14:21
> Paste this file into Claude Code, Codex, or Cursor and let it scaffold the full repository.

---

## 1. Overview

| Field | Value |
|:---|:---|
| **Project Name** | `Python-app` |
| **Type** | `fullstack` |
| **Primary Language** | `Python` |
| **Description** | *python-demo* |

---

## 2. Frontend

| Field | Value |
|:---|:---|
| **Framework** | `Nuxt 3` |
| **Styling** | `Tailwind CSS + Nuxt UI` |
| **State Management** | `Pinia` |
| **Data Fetching** | `Nuxt useFetch` |
| **Form Handling** | `VeeValidate + Zod` |

### Additional Tooling
- Dark Mode

### Required Environment Variables

```env
# frontend/.env.local
NUXT_PUBLIC_API_URL=http://localhost:8000/api
```

> **SSR Note** (Next.js / Nuxt / SvelteKit): server-side fetches inside Docker must use the backend service name,
> e.g. `http://backend:8000/api`. Client-side fetches always use `localhost`.

### Cookie Credentials Transmission (mandatory for auth)
- Set client-side fetching clients to send cookies automatically with requests:
  - **Axios**: set `axios.defaults.withCredentials = true` globally or pass `{ withCredentials: true }` in Axios instance options.
  - **Fetch API**: pass `{ credentials: 'include' }` in options block.
  - **SWR**: configure the fetcher parameter to pass `{ credentials: 'include' }`.
  - **TanStack Query**: configure query/mutation fetch functions to use credentials options.

### Framework Bootstrap Command
Execute this command to scaffold the frontend project framework:
```bash
npx nuxi@latest init frontend --packageManager npm --gitInit false
```

### Design System Adapter Bindings
- Read colors, spacing, and typography from `shared_tokens.md` before writing any CSS.
- **W-4 FIX**: Convert HEX values in `shared_tokens.md` to CSS HSL fragments so Tailwind maps correctly.
- Implement a `data-theme` dark/light toggle wired to the token system.

---

## 3. Backend

| Field | Value |
|:---|:---|
| **Framework** | `FastAPI` |
| **API Style** | `REST` |
| **Validation** | `Pydantic v2` |

### Features Enabled
- Health Checks
- Rate Limiting
- Redis Caching

### Required Environment Variables

```env
# backend/.env
DATABASE_URL=mysql://user:password@localhost:3306/appdb
JWT_SECRET=change-me-in-production
PORT=8000
REDIS_URL=redis://localhost:6379/0
```

### Framework Bootstrap Command
Execute this command to scaffold the backend project framework:
```bash
mkdir backend && cd backend && python -m venv venv && source venv/bin/activate (or venv\Scripts\activate) && pip install fastapi uvicorn pydantic
```

### Environment Variables Validation
```python
# Validate environment variables using pydantic-settings or custom checks
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    DATABASE_URL: str
    JWT_SECRET: str
    PORT: int = 8000

    class Config:
        env_file = ".env"

settings = Settings()
```

### Module Pattern
```
backend/app/routers/{resource}.py
backend/app/schemas/{resource}.py
backend/app/models/{resource}.py
backend/app/core/             # config, security, dependencies
```

### Security & Transport Baselines
- **Security Middleware**: Configure custom middleware or `CORSMiddleware` to inject standard security headers (X-Frame-Options, X-Content-Type-Options, Strict-Transport-Security).


### Feature Integration Instructions
> These features must be **wired up with actual code**, not just installed as packages.
#### Redis Caching
- Install: `redis`
- Create `backend/app/core/redis.py` — export a singleton async Redis client (`redis.asyncio`) initialized from `REDIS_URL`.
- Import and use the Redis client in routers and services.
#### Redis Connection Resilience
- **Auto-Reconnect**: Configure client options or connection pool settings to automatically retry connections on drop (e.g. using `retry_on_timeout=True` or Celery broker reconnect settings).
#### Rate Limiting
- Install: `slowapi`
- Set up a `Limiter` in `app/core/security.py` using client IP address and wire the middleware to the FastAPI app.
- Apply the `@limiter.limit("5/15minute")` decorator to auth route definitions.

### Health Endpoint (L-2 Rule)
- Route: `GET /api/health` → `{ status: "ok" }` with HTTP 200
- **Verify this returns 200 before writing a single frontend component.**

---

## 4. Database

| Field | Value |
|:---|:---|
| **Databases** | `MySQL` |
| **ORM / Query Layer** | `SQLAlchemy` |
| **Migrations** | `Yes` |
| **Seed Scripts** | `Yes` |

### Connection Rules
- Use `mysql+aiomysql://` for async SQLAlchemy with MySQL, `mysql+mysqlclient://` for sync.





### Connection Pooling Configurations
To comply with production performance standards, configure connection pooling:
- **SQLAlchemy**: Configure engine pools with limits:
  ```python
  engine = create_async_engine(
      DATABASE_URL,
      pool_size=10,
      max_overflow=20,
      pool_recycle=1800
  )
  ```

### Migration Commands
- Run: `alembic upgrade head`
- Run seed scripts immediately after migrations.

### Docker Compose DB Reference
```yaml
services:
  db:
    image: mysql:8
    environment:
      MYSQL_DATABASE: appdb
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_USER: appuser
      MYSQL_PASSWORD: apppassword
    ports:
      - "3306:3306"
```

---

## 5. Authentication & Authorization

| Field | Value |
|:---|:---|
| **Strategy** | `Hybrid Multi-Auth` |
| **Implementation** | `Custom FastAPI Jose/Passlib` |
| **RBAC** | `Yes` |
| **Multi-Tenant** | `Yes` |

### Auth Methods
- Google OAuth

### Required Environment Variables

```env
# backend/.env
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# No additional provider env vars — handled by JWT_SECRET above.
```

### Implementation Rules
- Protect all non-public routes with auth guards/middleware.
- Store tokens in HTTP-only cookies — never in localStorage. Set the `HttpOnly`, `Secure` (production only), and `SameSite=Strict` flags on the auth cookie to prevent session leakage and hijacking.
- Refresh tokens must be rotated on every use.
- Multi-tenant: scope every DB query to the resolved `tenantId`. Cross-tenant access is a critical security bug.

### Frontend Auth UI Requirements
> Implement **every** selected auth method with its own distinct UI. Do not default to email+password for all methods.
- **Google OAuth**: "Continue with Google" branded button with Google logo. Redirects to `/api/auth/google`; handles callback at `/api/auth/google/callback`.


### Multi-Tenant Interceptor Registration (mandatory)
- **NestJS**: add `{ provide: APP_INTERCEPTOR, useClass: TenantInterceptor }` to the `providers` array in `app.module.ts`. Import `APP_INTERCEPTOR` from `@nestjs/core`.
- **Express / Fastify / Hono**: register `tenantMiddleware` **before** all route handlers in `server.ts` — `app.use(tenantMiddleware)`.
- **FastAPI**: implement a `get_tenant_id` dependency to retrieve `tenant_id` from the `X-Tenant-ID` header or JWT claims, and filter SQLAlchemy database queries.
- **Django**: register a `TenantMiddleware` that extracts `tenant_id` and stores it in thread-local storage/context vars, and subclass `TenantModel` using a custom manager to auto-filter queries by `tenant_id`.
- Apply `@CurrentTenant()` decorator (or equivalent framework dependency/middleware extraction) in every controller or resolver that queries tenant-scoped data.
- Every DB query touching tenant data **must** be scoped to `tenantId`. A query without a tenant scope is a critical security bug.


### Roles (RBAC)
Define and enforce these roles via guards/middleware:
- admin
- manager
- editor
- viewer
- guest

---

## 6. Infrastructure & DevOps

| Field | Value |
|:---|:---|
| **Cloud / Platform** | `Azure` |
| **Terraform (IaC)** | `Yes` |
| **Docker** | `Yes` |
| **CI/CD** | `Yes — GitHub Actions` |

### Docker Setup
- Multi-stage Dockerfile
- docker-compose
- .dockerignore
- Docker Healthchecks

### Docker Port Rules (C-1 — mandatory)
- Frontend: `EXPOSE 3000` — process must listen on port 3000
- Backend: `EXPOSE 8000` — process must listen on port 8000
- Use service names (`backend`, `db`, `redis`) for inter-container communication, never `localhost`
- Add `depends_on: { <service>: { condition: service_healthy } }` to avoid race conditions

### Docker Compose Template

Place this file at the **project root** as `docker-compose.yml`.
The agent **must not** modify service names, internal hostnames, or port mappings.

```yaml
services:
  db:
    image: mysql:8
    container_name: app_database
    ports:
      - '3306:3306'
    environment:
      MYSQL_DATABASE: appdb
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_USER: appuser
      MYSQL_PASSWORD: apppassword
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7-alpine
    container_name: app_redis
    ports:
      - '6379:6379'
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
    container_name: app_backend
    command: sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000"
    ports:
      - '8000:8000'
    env_file:
      - ./backend/.env
    environment:
      - DATABASE_URL=mysql://appuser:apppassword@db:3306/appdb
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
    container_name: app_frontend
    ports:
      - '3000:3000'
    environment:
      - NUXT_PUBLIC_API_URL=http://backend:8000/api
    depends_on:
      - backend

volumes:
  mysql_data:

```

> **C-1 Rule**: All inter-container hostnames must use Docker service names (`backend`, `db`, `redis`, `mongo`, `keycloak`),
> never `localhost`. Frontend SSR fetches inside Docker also use service names, not `localhost`.

### .dockerignore Templates
Configure `.dockerignore` files to prevent transferring large directories like `node_modules` and local environment files during Docker builds.

#### backend/.dockerignore
```text
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.env
.env.*
```

#### frontend/.dockerignore
```text
node_modules
.next
out
npm-debug.log
.env.local
.env.development.local
.env.production.local
```

### CI/CD Pipeline (GitHub Actions)
Create this configuration at the root of your project.

```yaml
# .github/workflows/ci.yml
name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
      - name: Install Dependencies
        run: pip install -r requirements.txt
      - name: Run Linters
        run: pip install ruff && ruff check .
      - name: Type Check
        run: pip install mypy && mypy .
      - name: Run Unit Tests
        run: pytest
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Build Docker Images
        run: |
          docker build -t backend-service:latest ./backend
          docker build -t frontend-service:latest ./frontend
      - name: Deploy to Staging
        run: echo "Deploying to staging environment..."
      - name: Deploy to Production
        run: echo "Deploying to production environment..."
```

### Terraform Infrastructure Configuration

Place the IaC manifest in the `terraform/` directory.

```hcl
# terraform/main.tf
provider "azurerm" {
  features {}
}

terraform {
  required_version = ">= 1.5.0"
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 3.0"
    }
  }
}

resource "azurerm_resource_group" "main" {
  name     = "app-resources"
  location = "East US"
}
```

---

## 7. Testing & Quality Assurance

| Field | Value |
|:---|:---|
| **Testing Framework** | `pytest` |
| **Coverage Enforced** | `No` |

### Test Types
- Unit tests
- Integration tests
- API tests




---

## 8. Project Directory Structure

The agent **must** scaffold the repository using exactly this folder layout.
Do not invent directories or deviate from this structure without explicit instruction.

```text
python-app/
├── CLAUDE.md
├── SPEC.md
├── shared_tokens.md
├── .env.example
├── .gitignore
├── README.md
├── assets/
│   ├── logo.svg
│   ├── logo-mark.svg
│   ├── favicon.ico
│   └── fonts/
├── docker-compose.yml
├── .github/
│   └── workflows/
│       └── ci.yml
├── terraform/
│   ├── main.tf
│   ├── variables.tf
│   ├── outputs.tf
│   └── modules/
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── core/
│   │   │   ├── config.py
│   │   │   └── security.py
│   │   ├── routers/
│   │   │   ├── health.py
│   │   │   ├── auth.py
│   │   │   └── users.py
│   │   ├── schemas/
│   │   ├── models/
│   │   └── db/
│   │       ├── session.py
│   │       └── base.py
│   ├── alembic/
│   │   ├── env.py
│   │   └── versions/
│   ├── test/
│   ├── .env.example
│   ├── requirements.txt
│   ├── pyproject.toml
│   ├── Dockerfile
│   └── .dockerignore
└── frontend/
    ├── pages/
    │   ├── index.vue
    │   ├── login.vue
    │   └── dashboard.vue
    ├── components/
    ├── composables/
    ├── server/
    │   └── api/
    ├── stores/
    ├── layouts/
    ├── assets/
    │   └── css/
    ├── public/
    ├── .env.example
    ├── nuxt.config.ts
    └── tsconfig.json
    ├── Dockerfile
    └── .dockerignore
```

> Directory and file names with `{resource}` or `{module}` are placeholders.
> Replace them with actual domain entity names (e.g. `products/`, `orders/`).

---

## 9. AI Scaffold Instructions

You are an expert full-stack systems engineering agent.
Read `CLAUDE.md`, `SPEC.md`, and `shared_tokens.md` **completely** before writing a single line of code.
Scaffold the repository following this pipeline exactly — do not skip steps or reorder them.

### Pipeline

1. **Root Foundations**
   - Create `.gitignore`, `README.md`, root `package.json` / `pyproject.toml`.
   - Generate `backend/.env.example` and `frontend/.env.local.example` with all variables from Sections 3 and 5.


2. **Database Schema** *(MySQL via SQLAlchemy)*
   - Write schema / model files.
   - Generate initial migration.
   - Write deterministic seed scripts.

3. **Core Backend — L-2 Rule** *(FastAPI)*
   - Scaffold app bootstrap, module structure, shared middleware.
   - Implement `GET /api/health` → `{ status: "ok" }`.
   - **⛔ Do not write frontend code until `curl http://localhost:8000/api/health` returns HTTP 200.**

4. **Auth Layer** *(Custom FastAPI Jose/Passlib — Google OAuth)*
   - Wire up Custom FastAPI Jose/Passlib with: Google OAuth.
   - Apply auth guards to all non-public routes.
   - Implement RBAC guards for roles: admin, manager, editor, viewer, guest.

5. **Frontend Scaffold** *(Nuxt 3)*
   - Bootstrap with non-interactive CLI flags.
   - Apply design tokens from `shared_tokens.md` — convert HEX → HSL.
   - Implement dark/light theme toggle via `data-theme`.
   - Dashboard shell with live connection indicator calling `/api/health`.

6. **Docker Orchestration** *(enabled)*
   - Copy the `docker-compose.yml` template from Section 6 **verbatim** to the project root.
   - Do not rename services, change internal hostnames, or alter port mappings.
   - Write multi-stage Dockerfiles: frontend `EXPOSE 3000`, backend `EXPOSE 8000`.
   - Write `.dockerignore` files for both backend and frontend as detailed in Section 6 to ignore `node_modules`, environment files, and build outputs.

7. **CI/CD** *(GitHub Actions — Lint, Type check, Unit tests, E2E tests, Docker build & push, Deploy staging, Deploy production)*
   - Generate pipeline config running: Lint, Type check, Unit tests, E2E tests, Docker build & push, Deploy staging, Deploy production.

8. **Tests** *(pytest — Unit tests, Integration tests, API tests)*
   - Scaffold test files alongside source files.
   

9. **README.md**
   - Local setup: clone → install → env setup → migrate → run
   - All environment variables documented
   - How to run tests and Docker

### Critical Rules
- Never hardcode secrets — use environment variables exclusively.
- Never use `localhost` as a hostname inside Docker containers — use service names.
- Follow the L-2 rule: backend `/health` endpoint verified before any frontend code is written.
- Follow Section 8 directory structure exactly.
- For Docker: copy the compose template from Section 6 verbatim — do not invent or remove services.
- For container self-healthchecks (e.g. Dockerfile `HEALTHCHECK` command), use `127.0.0.1` instead of `localhost` to ensure successful IPv4 loopback resolution in Alpine Linux environments.
- For package installation: when writing Dockerfiles or running dependency installations, always use `npm ci --legacy-peer-deps` and `npm prune --omit=dev --legacy-peer-deps` to prevent peer dependency resolution conflicts.
- For NestJS/TypeScript backends: explicitly define `"rootDir": "src"` and `"incremental": false` under `"compilerOptions"` in `tsconfig.build.json` (and exclude config files like `vitest.config.ts`) to ensure the compiled JS goes straight to `dist/` (not nested inside `dist/src/`) and build caching issues are avoided inside containers.
- For OAuth authentication: ensure NestJS/Passport strategies (like Google, Azure AD, GitHub, etc.) check for empty environment variables and fall back to dummy/placeholder strings (e.g. 'dummy-google-client-id', 'dummy-azure-client-id') during construction to prevent the server from crashing on boot in local development.
- For Next.js rewrites: when proxying `/api` or `/api/auth` to the backend, resolve the target URL using `process.env.NEXT_PUBLIC_API_URL_INTERNAL || (isDocker ? 'http://backend:8000' : 'http://localhost:8000')` (where `isDocker` is checked via `fs.existsSync('/.dockerenv')`) so that rewrites work correctly during both local host development and container-internal build stages.