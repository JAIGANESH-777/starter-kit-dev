# Project Spec: my-saas-app

> **Generated by Accelerator Enterprise Architecture Compiler** on 11 Jun 2026, 14:31
> Paste this file into Claude Code, Codex, or Cursor and let it scaffold the full repository.

---

## 1. Overview

| Field | Value |
|:---|:---|
| **Project Name** | `my-saas-app` |
| **Type** | `fullstack` |
| **Primary Language** | `TypeScript` |
| **Description** | *A full-stack SaaS platform* |

---

## 2. Frontend

| Field | Value |
|:---|:---|
| **Framework** | `Next.js` |
| **Styling** | `Tailwind CSS + shadcn/ui` |
| **State Management** | `Zustand` |
| **Data Fetching** | `TanStack Query + Axios` |
| **Form Handling** | `React Hook Form + Zod` |

### Additional Tooling
- Dark Mode
- Storybook

### Required Environment Variables

```env
# frontend/.env.local
NEXT_PUBLIC_API_URL=http://localhost:8000/api
```

> **SSR Note** (Next.js / Nuxt / SvelteKit): server-side fetches inside Docker must use the backend service name,
> e.g. `http://backend:8000/api`. Client-side fetches always use `localhost`.

### Design System Adapter Bindings
- Read colors, spacing, and typography from `shared_tokens.md` before writing any CSS.
- **W-4 FIX**: Convert HEX values in `shared_tokens.md` to CSS HSL fragments so Tailwind maps correctly.
- Implement a `data-theme` dark/light toggle wired to the token system.

---

## 3. Backend

| Field | Value |
|:---|:---|
| **Framework** | `NestJS` |
| **API Style** | `REST` |
| **Validation** | `class-validator` |

### Features Enabled
- Health Checks
- Swagger/OpenAPI Docs

### Required Environment Variables

```env
# backend/.env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/appdb
JWT_SECRET=change-me-in-production
PORT=8000
```

### Module Pattern
```
backend/src/{module}/{module}.controller.ts, .service.ts, .module.ts
backend/src/common/          # shared guards, filters, interceptors, pipes
backend/src/config/          # environment config module
```

### Health Endpoint (L-2 Rule)
- Route: `GET /api/health` → `{ status: "ok" }` with HTTP 200
- **Verify this returns 200 before writing a single frontend component.**

---

## 4. Database

| Field | Value |
|:---|:---|
| **Databases** | `PostgreSQL` |
| **ORM / Query Layer** | `Prisma` |
| **Migrations** | `Yes` |
| **Seed Scripts** | `Yes` |

### Connection Rules
- **C-2 FIX**: Use `postgresql://` (not `postgres://`) in `DATABASE_URL` — Prisma rejects the shorter prefix.

- In Docker, the DB host is the service name (e.g. `db`), not `localhost`.

### Migration Commands
- Run: `npx prisma migrate dev` (dev) / `npx prisma migrate deploy` (prod)
- Run seed scripts immediately after migrations.

### Docker Compose DB Reference
```yaml
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: appdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
```

---

## 5. Authentication & Authorization

| Field | Value |
|:---|:---|
| **Strategy** | `JWT + Refresh Tokens` |
| **Implementation** | `Auth.js` |
| **RBAC** | `Yes` |
| **Multi-Tenant** | `No` |

### Auth Methods
- Email/Password
- Google OAuth

### Required Environment Variables

```env
# backend/.env
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# frontend/.env.local
NEXTAUTH_SECRET=change-me-in-production
NEXTAUTH_URL=http://localhost:3000
```

### Implementation Rules
- Protect all non-public routes with auth guards/middleware.
- Store tokens in HTTP-only cookies — never in localStorage.
- Refresh tokens must be rotated on every use.

### Roles (RBAC)
Define and enforce these roles via guards/middleware:
- admin
- editor
- viewer

---

## 6. Infrastructure & DevOps

| Field | Value |
|:---|:---|
| **Cloud / Platform** | `AWS` |
| **Terraform (IaC)** | `Yes` |
| **Docker** | `Yes` |
| **CI/CD** | `Yes — GitHub Actions` |

### Docker Setup
- Multi-stage Dockerfile
- docker-compose

### Docker Port Rules (C-1 — mandatory)
- Frontend: `EXPOSE 3000` — process must listen on port 3000
- Backend: `EXPOSE 8000` — process must listen on port 8000
- Use service names (`backend`, `db`) for inter-container communication, never `localhost`
- Add `depends_on: { db: { condition: service_healthy } }` to avoid race conditions

### CI/CD Pipeline (GitHub Actions)
- Lint
- Type check
- Unit tests
- Build
- Docker build & push

### Code Quality Tools
- ESLint
- Prettier
- Husky
- lint-staged
- Commitlint

- **ESLint**: `eslint.config.mjs` (flat config). Use `@typescript-eslint/recommended` + framework plugin.
- **Prettier**: `.prettierrc` → `{ "singleQuote": true, "semi": true, "printWidth": 100 }`.
- **Husky**: `npx husky init` → add pre-commit hook to run lint-staged.
- **lint-staged**: in `package.json` → run ESLint + Prettier on staged `*.ts` / `*.tsx` files.
- **Commitlint**: `@commitlint/config-conventional` + commit-msg Husky hook.

### Terraform
- Provider: `AWS`
- Modules in `terraform/`: networking, compute, database, IAM
- Use remote state backend (S3 / Azure Blob / GCS)

---

## 7. Testing & Quality Assurance

| Field | Value |
|:---|:---|
| **Testing Framework** | `Vitest` |
| **Coverage Enforced** | `Yes` |

### Test Types
- Unit tests
- Integration tests

### Coverage Thresholds
Minimum 80% for lines, functions, and branches. Fail CI if not met.

---

## 9. Project Directory Structure

The agent **must** scaffold the repository using exactly this folder layout.
Do not invent directories or deviate from this structure without explicit instruction.

```text
my-saas-app/
├── CLAUDE.md
├── SPEC.md
├── shared_tokens.md
├── .env.example
├── .gitignore
├── README.md
├── eslint.config.mjs
├── .prettierrc
├── commitlint.config.js
├── .husky/
│   └── pre-commit
├── assets/
│   ├── logo.svg
│   ├── logo-mark.svg
│   ├── favicon.ico
│   └── fonts/
├── docker-compose.yml
├── .github/
│   └── workflows/
│       └── ci.yml
├── terraform/
│   ├── main.tf
│   ├── variables.tf
│   ├── outputs.tf
│   └── modules/
├── backend/
│   ├── src/
│   │   ├── main.ts
│   │   ├── app.module.ts
│   │   ├── common/
│   │   │   ├── guards/
│   │   │   ├── filters/
│   │   │   ├── interceptors/
│   │   │   └── pipes/
│   │   ├── config/
│   │   │   └── configuration.ts
│   │   ├── health/
│   │   │   ├── health.controller.ts
│   │   │   └── health.module.ts
│   │   ├── auth/
│   │   │   ├── auth.controller.ts
│   │   │   ├── auth.service.ts
│   │   │   ├── auth.module.ts
│   │   │   ├── dto/
│   │   │   └── strategies/
│   │   │       └── jwt.strategy.ts
│   │   └── users/
│   │       ├── users.controller.ts
│   │       ├── users.service.ts
│   │       ├── users.module.ts
│   │       └── dto/
│   ├── prisma/
│   │   ├── schema.prisma
│   │   └── migrations/
│   │   └── seed.ts
│   ├── test/
│   ├── .env.example
│   ├── package.json
│   ├── tsconfig.json
│   └── Dockerfile
└── frontend/
    ├── app/
    │   ├── layout.tsx
    │   ├── page.tsx
    │   ├── globals.css
    │   ├── (auth)/
    │   │   ├── login/
    │   │   │   └── page.tsx
    │   │   └── register/
    │   │       └── page.tsx
    │   └── (dashboard)/
    │       ├── layout.tsx
    │       └── page.tsx
    ├── components/
    │   ├── ui/
    │   ├── layouts/
    │   └── forms/
    ├── lib/
    │   ├── api.ts
    │   └── utils.ts
    ├── hooks/
    ├── store/
    ├── types/
    ├── public/
    ├── .env.local.example
    ├── next.config.ts
    ├── tsconfig.json
    ├── tailwind.config.ts
    └── Dockerfile
```

> Directory and file names with `{resource}` or `{module}` are placeholders.
> Replace them with actual domain entity names (e.g. `products/`, `orders/`).

---

## 10. AI Scaffold Instructions

You are an expert full-stack systems engineering agent.
Read `CLAUDE.md`, `SPEC.md`, and `shared_tokens.md` **completely** before writing a single line of code.
Scaffold the repository following this pipeline exactly — do not skip steps or reorder them.

### Pipeline

1. **Root Foundations**
   - Create `.gitignore`, `README.md`, root `package.json` / `pyproject.toml`.
   - Generate `backend/.env.example` and `frontend/.env.local.example` with all variables from Sections 3 and 5.
   - Configure code quality tools: ESLint, Prettier, Husky, lint-staged, Commitlint.

2. **Database Schema** *(PostgreSQL via Prisma)*
   - Write schema / model files.
   - Generate initial migration.
   - Write deterministic seed scripts.

3. **Core Backend — L-2 Rule** *(NestJS)*
   - Scaffold app bootstrap, module structure, shared middleware.
   - Implement `GET /api/health` → `{ status: "ok" }`.
   - **⛔ Do not write frontend code until `curl http://localhost:8000/api/health` returns HTTP 200.**

4. **Auth Layer** *(Auth.js — Email/Password, Google OAuth)*
   - Wire up Auth.js with: Email/Password, Google OAuth.
   - Apply auth guards to all non-public routes.
   - Implement RBAC guards for roles: admin, editor, viewer.

5. **Frontend Scaffold** *(Next.js)*
   - Bootstrap with non-interactive CLI flags.
   - Apply design tokens from `shared_tokens.md` — convert HEX → HSL.
   - Implement dark/light theme toggle via `data-theme`.
   - Dashboard shell with live connection indicator calling `/api/health`.

6. **Docker Orchestration** *(enabled)*
   - Multi-stage Dockerfiles: frontend `EXPOSE 3000`, backend `EXPOSE 8000`.
   - `docker-compose.yml`: all services wired with healthchecks and service-name references.

7. **CI/CD** *(GitHub Actions — Lint, Type check, Unit tests, Build, Docker build & push)*
   - Generate pipeline config running: Lint, Type check, Unit tests, Build, Docker build & push.

8. **Tests** *(Vitest — Unit tests, Integration tests)*
   - Scaffold test files alongside source files.
   - Enforce 80% coverage thresholds in CI.

9. **README.md**
   - Local setup: clone → install → env setup → migrate → run
   - All environment variables documented
   - How to run tests and Docker

### Critical Rules
- Never hardcode secrets — use environment variables exclusively.
- Never use `localhost` as a hostname inside Docker containers — use service names.
- Follow the L-2 rule: backend `/health` endpoint verified before any frontend code is written.
- Follow Section 9 directory structure exactly.
